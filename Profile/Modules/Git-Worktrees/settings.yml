username: ken
default-branch: master
repository-commands:
  - quest: 
    - Write-Host 'Copying .vs folder' ; Copy-Item "$((Get-Worktrees)['master'])\.vs" . -Recurse -Force
    - cd ui
    - npm install
    - cd ..
